---
phase: "03"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/tailwind.config.ts
  - apps/web/postcss.config.mjs
  - apps/web/tsconfig.json
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/page.tsx
  - apps/web/src/app/globals.css
  - apps/web/src/app/providers/tauri-provider.tsx
  - apps/web/src/components/AppShell.tsx
  - apps/web/src/components/Sidebar.tsx
  - apps/web/src/components/SidebarHeader.tsx
  - apps/web/src/components/SidebarNav.tsx
  - apps/web/src/components/ui/Button.tsx
  - apps/web/src/components/ui/Input.tsx
  - apps/web/src/components/ui/Card.tsx
autonomous: true
requirements:
  - UI-04
  - UI-05
must_haves:
  truths:
    - "Next.js app renders within the Tauri window"
    - "Sidebar displays 4 navigation items: Status, Providers, Model Mapping, Settings"
    - "Clicking a nav item updates the active page without full reload"
    - "Active nav item is visually distinguished with orange left border"
    - "Cursor brand colors applied: cream canvas (#f7f7f4), white cards (#ffffff), orange accent (#f54e00)"
  artifacts:
    - path: "apps/web/package.json"
      provides: "Next.js 15 app with Tauri, Tailwind, Lucide dependencies"
      contains: '"next": "^15"'
    - path: "apps/web/tailwind.config.ts"
      provides: "Cursor brand design tokens as Tailwind theme"
      contains: "colors: { canvas: '#f7f7f4'"
    - path: "apps/web/src/app/globals.css"
      provides: "Tailwind directives + Inter + JetBrains Mono font imports"
      contains: "@tailwind base"
    - path: "apps/web/src/components/AppShell.tsx"
      provides: "Root layout with 240px sidebar + content area"
      exports: "AppShell"
    - path: "apps/web/src/components/SidebarNav.tsx"
      provides: "Navigation with 4 items and active state"
      contains: "Status"
    - path: "apps/web/src/app/layout.tsx"
      provides: "Root layout wrapping content in AppShell"
      exports: "default function RootLayout"
  key_links:
    - from: "apps/web/src/app/layout.tsx"
      to: "apps/web/src/components/AppShell.tsx"
      via: "import AppShell"
      pattern: "import.*AppShell"
    - from: "apps/web/src/components/AppShell.tsx"
      to: "apps/web/src/components/Sidebar.tsx"
      via: "Sidebar rendered inside AppShell"
      pattern: "<Sidebar"
    - from: "apps/web/src/components/SidebarNav.tsx"
      to: "apps/web/src/app/page.tsx"
      via: "nav item click → router.push('/')"
      pattern: "router\\.push"
---

<objective>
Initialize Next.js 15 frontend within Tauri with Cursor brand design system, AppShell layout, and sidebar navigation. Creates the visual foundation that all subsequent UI pages build upon.

Purpose: Provide the desktop application's user interface shell — the layout, navigation, and design system that makes this a Cursor-branded desktop app rather than a bare web page. Without this plan, there is no UI for the user to interact with.
Output: Next.js 15 app with Tailwind Cursor brand tokens, AppShell with 240px sidebar, 4-page navigation (Status, Providers, Model Mapping, Settings), reusable UI components (Button, Input, Card).
</objective>

<execution_context>
@/Users/davidefeltrin/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/davidefeltrin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-desktop-ui-shell/03-CONTEXT.md
@.planning/phases/03-desktop-ui-shell/03-UI-SPEC.md
@DESIGN.md
@.planning/research/STACK.md
</context>

<interfaces>
<!-- Design tokens the executor must implement exactly -->

Cursor brand colors (from DESIGN.md + UI-SPEC):
```
canvas:        #f7f7f4  (dominant 60% — page background)
surface-card:  #ffffff  (secondary 30% — sidebar, cards, forms)
primary:       #f54e00  (accent 10% — CTAs only: Save, Test Connection, Start Proxy)
primary-active:#d04200  (press state for primary buttons)
ink:           #26251e  (text primary — display, body, emphasis)
body:          #5a5852  (text secondary — default running text)
muted:         #807d72  (text muted — sub-titles, stopped status)
hairline:      #e6e5e0  (1px divider)
hairline-strong: #cfcdc4 (stronger panel outline)
semantic-success: #1f8a65 (running status, validation success)
semantic-error:   #cf2d56 (error states, destructive actions)
```

Typography (from UI-SPEC — 4 sizes, Inter + JetBrains Mono):
```
Display: 22px / 400 / 1.3 / -0.11px — page titles
Heading: 18px / 600 / 1.4 / 0 — section headings
Body:    16px / 400 / 1.5 / 0 — default text, labels, buttons
Small:   14px / 400 / 1.5 / 0 — helper text, code surfaces (JetBrains Mono)
```

Spacing (from UI-SPEC):
```
xxs: 4px   — icon gaps, status dot
xs:  8px   — compact padding, form field gaps
md:  16px  — card padding, default spacing
lg:  24px  — form group spacing, layout gaps
xl:  32px  — major section breaks
2xl: 48px  — page header spacing
```

Border radius:
```
xs: 4px   — inline tags
sm: 6px   — compact rows
md: 8px   — buttons, form inputs
lg: 12px  — cards
pill: 9999px — status dots, badges
```

Sidebar: 240px fixed width
Window: 1200x800 default, 900x600 minimum
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 15 app with Tailwind CSS and Cursor brand design tokens</name>
  <read_first>
    - DESIGN.md (complete Cursor brand system — colors, typography, spacing, shapes)
    - .planning/phases/03-desktop-ui-shell/03-UI-SPEC.md (spacing scale, typography roles, color usage)
    - .planning/research/STACK.md (Next.js 15 installation, Tailwind setup)
  </read_first>
  <acceptance_criteria>
    - apps/web/package.json exists with "next": "^15.0.0", "react": "^19.0.0", "react-dom": "^19.0.0"
    - apps/web/package.json has "@tauri-apps/api": "^2.0.0", "lucide-react", "clsx", "tailwind-merge"
    - apps/web/tailwind.config.ts exists with theme.colors containing: canvas '#f7f7f4', primary '#f54e00', ink '#26251e', body '#5a5852', muted '#807d72', hairline '#e6e5e0', 'hairline-strong' '#cfcdc4', 'surface-card' '#ffffff', 'semantic-success' '#1f8a65', 'semantic-error' '#cf2d56'
    - apps/web/tailwind.config.ts has theme.fontFamily with sans: ['Inter', 'system-ui', 'sans-serif'] and mono: ['JetBrains Mono', 'monospace']
    - apps/web/tailwind.config.ts has theme.fontSize with display: ['22px', { lineHeight: '1.3', letterSpacing: '-0.11px' }], heading: ['18px', { lineHeight: '1.4', fontWeight: '600' }], body: ['16px', { lineHeight: '1.5' }], small: ['14px', { lineHeight: '1.5' }]
    - apps/web/tailwind.config.ts has theme.spacing with xxs: '4px', xs: '8px', md: '16px', lg: '24px', xl: '32px', '2xl': '48px'
    - apps/web/tailwind.config.ts has theme.borderRadius with xs: '4px', sm: '6px', md: '8px', lg: '12px', pill: '9999px'
    - apps/web/src/app/globals.css contains @tailwind base, @tailwind components, @tailwind utilities
    - apps/web/src/app/globals.css contains @import for Inter and JetBrains Mono from Google Fonts or local
    - apps/web/next.config.ts exists with output: 'export' or standard config
    - apps/web/tsconfig.json exists with "jsx": "preserve" and Next.js types
    - `cd apps/web && npm run build` exits 0 (or at least `npx next lint` exits 0)
  </acceptance_criteria>
  <action>
    1. Create Next.js 15 app in apps/web/:
    ```bash
    npx create-next-app@latest apps/web --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm
    ```
    Select: TypeScript yes, Tailwind yes, ESLint yes, App Router yes, src/ directory yes.

    2. Install additional dependencies:
    ```bash
    cd apps/web
    npm install @tauri-apps/api@latest lucide-react clsx tailwind-merge
    ```

    3. Configure tailwind.config.ts with Cursor brand tokens. Replace the entire content:
    ```typescript
    import type { Config } from "tailwindcss";

    export default {
      content: [
        "./src/pages/**/*.{js,ts,jsx,tsx,mdx}",
        "./src/components/**/*.{js,ts,jsx,tsx,mdx}",
        "./src/app/**/*.{js,ts,jsx,tsx,mdx}",
      ],
      theme: {
        extend: {
          colors: {
            canvas: '#f7f7f4',
            'canvas-soft': '#fafaf7',
            primary: '#f54e00',
            'primary-active': '#d04200',
            ink: '#26251e',
            body: '#5a5852',
            muted: '#807d72',
            'muted-soft': '#a09c92',
            hairline: '#e6e5e0',
            'hairline-soft': '#efeee8',
            'hairline-strong': '#cfcdc4',
            'surface-card': '#ffffff',
            'surface-strong': '#e6e5e0',
            'semantic-success': '#1f8a65',
            'semantic-error': '#cf2d56',
          },
          fontFamily: {
            sans: ['Inter', 'system-ui', 'sans-serif'],
            mono: ['JetBrains Mono', 'monospace'],
          },
          fontSize: {
            display: ['22px', { lineHeight: '1.3', letterSpacing: '-0.11px', fontWeight: '400' }],
            heading: ['18px', { lineHeight: '1.4', letterSpacing: '0', fontWeight: '600' }],
            body: ['16px', { lineHeight: '1.5', letterSpacing: '0', fontWeight: '400' }],
            small: ['14px', { lineHeight: '1.5', letterSpacing: '0', fontWeight: '400' }],
          },
          spacing: {
            xxs: '4px',
            xs: '8px',
            md: '16px',
            lg: '24px',
            xl: '32px',
            '2xl': '48px',
          },
          borderRadius: {
            xs: '4px',
            sm: '6px',
            md: '8px',
            lg: '12px',
            pill: '9999px',
          },
        },
      },
      plugins: [],
    } satisfies Config;
    ```

    4. Update apps/web/src/app/globals.css:
    ```css
    @tailwind base;
    @tailwind components;
    @tailwind utilities;

    @layer base {
      * {
        @apply border-hairline;
      }
      body {
        @apply bg-canvas text-ink font-sans text-body antialiased;
      }
    }

    @layer components {
      .focus-ring {
        @apply focus:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 focus-visible:ring-offset-canvas;
      }
    }
    ```

    5. Add Google Fonts import to apps/web/src/app/layout.tsx (will be further modified in Task 2):
    ```typescript
    import { Inter } from 'next/font/google';

    const inter = Inter({
      subsets: ['latin'],
      variable: '--font-inter',
      display: 'swap',
    });
    ```

    6. Update apps/web/next.config.ts for Tauri compatibility:
    ```typescript
    import type { NextConfig } from "next";

    const nextConfig: NextConfig = {
      output: 'export',
      trailingSlash: true,
      images: {
        unoptimized: true,
      },
    };

    export default nextConfig;
    ```

    Note: For Tauri dev mode, we'll use the dev server (not export). The `output: 'export'` is for production builds. In dev, Next.js serves normally at localhost:3000.

    7. Verify the app compiles:
    ```bash
    cd apps/web && npx next lint 2>&1 | head -20
    ```
  </action>
  <verify>
    <automated>cd apps/web && test -f package.json && test -f tailwind.config.ts && test -f src/app/globals.css && test -f src/app/layout.tsx && grep -q "canvas.*#f7f7f4" tailwind.config.ts && grep -q "primary.*#f54e00" tailwind.config.ts && echo "Next.js + Tailwind + Cursor brand configured" || echo "MISSING files or tokens"</automated>
  </verify>
  <done>Next.js 15 app created at apps/web/ with Tailwind CSS configured with full Cursor brand design tokens (colors, fonts, spacing, border radius), Google Fonts Inter loaded, Tauri-compatible next.config.ts</done>
</task>

<task type="auto">
  <name>Task 2: Build AppShell, Sidebar, and navigation with Cursor brand styling</name>
  <read_first>
    - .planning/phases/03-desktop-ui-shell/03-UI-SPEC.md (component inventory, layout specs, interaction contracts)
    - DESIGN.md (spacing, shapes, elevation rules — hairline only, no shadows)
    - apps/web/tailwind.config.ts (just created — token names to use)
  </read_first>
  <acceptance_criteria>
    - apps/web/src/components/AppShell.tsx exports default function AppShell({ children }) 
    - AppShell renders a flex container with min-h-screen, bg-canvas
    - AppShell contains Sidebar (240px fixed width, bg-surface-card, border-r border-hairline)
    - AppShell contains main content area with flex-1, bg-canvas, p-lg (24px padding)
    - apps/web/src/components/Sidebar.tsx exports default function Sidebar()
    - Sidebar contains SidebarHeader + SidebarNav in vertical flex
    - apps/web/src/components/SidebarHeader.tsx exports default function SidebarHeader()
    - SidebarHeader displays "ClaudeCode Proxy" text (font-display, 22px) + StatusDot component placeholder
    - SidebarHeader has height 56px, border-b border-hairline, px-md (16px)
    - apps/web/src/components/SidebarNav.tsx exports default function SidebarNav()
    - SidebarNav renders exactly 4 nav items: "Status", "Providers", "Model Mapping", "Settings"
    - Each nav item has icon from lucide-react: Status=Activity, Providers=Server, Model Mapping=Route, Settings=Settings
    - Active nav item has border-l-2 border-primary (orange left border) and bg-canvas-soft
    - Inactive nav items have text-body color, hover:bg-canvas-soft
    - Nav items use router.push() for client-side navigation
    - apps/web/src/app/layout.tsx wraps children in <AppShell>{children}</AppShell>
    - apps/web/src/app/page.tsx renders as Status page placeholder (will be replaced by Plan 03)
  </acceptance_criteria>
  <action>
    Create the following component files:

    **apps/web/src/components/AppShell.tsx:**
    ```tsx
    import { Sidebar } from './Sidebar';

    interface AppShellProps {
      children: React.ReactNode;
    }

    export function AppShell({ children }: AppShellProps) {
      return (
        <div className="flex min-h-screen bg-canvas">
          <Sidebar />
          <main className="flex-1 bg-canvas p-lg">
            {children}
          </main>
        </div>
      );
    }
    ```

    **apps/web/src/components/Sidebar.tsx:**
    ```tsx
    import { SidebarHeader } from './SidebarHeader';
    import { SidebarNav } from './SidebarNav';

    export function Sidebar() {
      return (
        <aside className="w-60 bg-surface-card border-r border-hairline flex flex-col">
          <SidebarHeader />
          <SidebarNav />
        </aside>
      );
    }
    ```

    **apps/web/src/components/SidebarHeader.tsx:**
    ```tsx
    import { StatusDot } from './StatusDot';

    export function SidebarHeader() {
      return (
        <header className="h-14 px-md border-b border-hairline flex items-center justify-between">
          <h1 className="font-display text-ink text-[22px] leading-[1.3] tracking-[-0.11px] font-normal">
            ClaudeCode Proxy
          </h1>
          <StatusDot state="loading" />
        </header>
      );
    }
    ```

    Note: StatusDot will be created in Plan 03. For now, create a minimal placeholder:

    **apps/web/src/components/StatusDot.tsx (placeholder for Plan 03):**
    ```tsx
    interface StatusDotProps {
      state: 'running' | 'stopped' | 'error' | 'loading';
    }

    const stateColors: Record<StatusDotProps['state'], string> = {
      running: 'bg-semantic-success',
      stopped: 'bg-muted',
      error: 'bg-semantic-error',
      loading: 'bg-primary animate-pulse',
    };

    export function StatusDot({ state }: StatusDotProps) {
      return (
        <div
          className={`w-2 h-2 rounded-pill ${stateColors[state]}`}
          aria-label={`Proxy status: ${state}`}
          role="status"
        />
      );
    }
    ```

    **apps/web/src/components/SidebarNav.tsx:**
    ```tsx
    'use client';

    import { Activity, Server, Route, Settings } from 'lucide-react';
    {`import { usePathname, useRouter } from 'next/navigation';`}

    const navItems = [
      { label: 'Status', href: '/', icon: Activity },
      { label: 'Providers', href: '/providers', icon: Server },
      { label: 'Model Mapping', href: '/mapping', icon: Route },
      { label: 'Settings', href: '/settings', icon: Settings },
    ];

    export function SidebarNav() {
      const pathname = usePathname();
      const router = useRouter();

      return (
        <nav className="flex-1 py-xs" aria-label="Main navigation">
          <ul className="space-y-xxs">
            {navItems.map(({ label, href, icon: Icon }) => {
              const isActive = pathname === href;
              return (
                <li key={href}>
                  <button
                    onClick={() => router.push(href)}
                    className={`
                      w-full flex items-center gap-xs px-md py-xs text-body text-left focus-ring
                      ${isActive
                        ? 'border-l-2 border-primary bg-canvas-soft text-ink font-medium'
                        : 'border-l-2 border-transparent hover:bg-canvas-soft'
                      }
                    `}
                    aria-current={isActive ? 'page' : undefined}
                  >
                    <Icon className="w-4 h-4" aria-hidden="true" />
                    <span className="text-sm">{label}</span>
                  </button>
                </li>
              );
            })}
          </ul>
        </nav>
      );
    }
    ```

    **Update apps/web/src/app/layout.tsx:**
    ```tsx
    import { Inter } from 'next/font/google';
    import { AppShell } from '@/components/AppShell';
    import './globals.css';

    const inter = Inter({
      subsets: ['latin'],
      variable: '--font-inter',
      display: 'swap',
    });

    export const metadata = {
      title: 'ClaudeCode Proxy',
      description: 'Route Claude Code through optimal providers',
    };

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode;
    }) {
      return (
        <html lang="en" className={inter.variable}>
          <body>
            <AppShell>{children}</AppShell>
          </body>
        </html>
      );
    }
    ```

    **Update apps/web/src/app/page.tsx (placeholder — Plan 03 will replace with Status page):**
    ```tsx
    export default function HomePage() {
      return (
        <div>
          <h2 className="font-display text-[22px] text-ink mb-lg">Status</h2>
          <p className="text-body">Status page coming soon.</p>
        </div>
      );
    }
    ```

    **Create placeholder route pages for nav targets:**

    apps/web/src/app/providers/page.tsx:
    ```tsx
    export default function ProvidersPage() {
      return (
        <div>
          <h2 className="font-display text-[22px] text-ink mb-lg">Providers</h2>
          <p className="text-body">Provider configuration coming soon.</p>
        </div>
      );
    }
    ```

    apps/web/src/app/mapping/page.tsx:
    ```tsx
    export default function MappingPage() {
      return (
        <div>
          <h2 className="font-display text-[22px] text-ink mb-lg">Model Mapping</h2>
          <p className="text-body">Model mapping configuration coming soon.</p>
        </div>
      );
    }
    ```

    apps/web/src/app/settings/page.tsx:
    ```tsx
    export default function SettingsPage() {
      return (
        <div>
          <h2 className="font-display text-[22px] text-ink mb-lg">Settings</h2>
          <p className="text-body">Settings coming soon.</p>
        </div>
      );
    }
    ```
  </action>
  <verify>
    <automated>test -f apps/web/src/components/AppShell.tsx && test -f apps/web/src/components/Sidebar.tsx && test -f apps/web/src/components/SidebarHeader.tsx && test -f apps/web/src/components/SidebarNav.tsx && test -f apps/web/src/components/StatusDot.tsx && grep -q "Status" apps/web/src/components/SidebarNav.tsx && grep -q "Providers" apps/web/src/components/SidebarNav.tsx && grep -q "Model Mapping" apps/web/src/components/SidebarNav.tsx && grep -q "Settings" apps/web/src/components/SidebarNav.tsx && echo "All shell components exist with 4 nav items" || echo "MISSING shell components or nav items"</automated>
  </verify>
  <done>AppShell with 240px sidebar + content area built, SidebarHeader with app name + status dot, SidebarNav with 4 items (Status, Providers, Model Mapping, Settings) using Lucide icons, active state with orange left border, page placeholders created for all 4 routes</done>
</task>

<task type="auto">
  <name>Task 3: Create reusable UI components (Button, Input, Card) with Cursor brand styling</name>
  <read_first>
    - DESIGN.md (button-primary, button-secondary, text-input specs — heights, padding, radius)
    - .planning/phases/03-desktop-ui-shell/03-UI-SPEC.md (spacing, typography, component inventory)
    - apps/web/tailwind.config.ts (token names to reference)
  </read_first>
  <acceptance_criteria>
    - apps/web/src/components/ui/Button.tsx exports Button component with variants: primary, secondary, destructive, ghost
    - Button primary: bg-primary text-white (on-primary #ffffff), h-10 (40px), px-[18px], rounded-md (8px), font-medium text-sm
    - Button secondary: bg-surface-card text-ink, border border-hairline-strong, h-10, rounded-md
    - Button destructive: bg-semantic-error text-white, h-10, rounded-md
    - Button ghost: bg-transparent text-ink, hover:bg-canvas-soft, h-10
    - Button supports loading state: shows spinner, disabled, text changes to loadingText prop
    - Button has min-h-[44px] for WCAG AAA touch targets
    - apps/web/src/components/ui/Input.tsx exports Input component
    - Input: bg-surface-card text-ink, border border-hairline, rounded-md (8px), h-11 (44px), px-4 (16px), text-body
    - Input supports type="password" with show/hide toggle (eye icon from lucide-react)
    - Input supports error state: border-semantic-error + error message below
    - Input supports label prop that renders above the input
    - apps/web/src/components/ui/Card.tsx exports Card component
    - Card: bg-surface-card, rounded-lg (12px), border border-hairline, p-md (16px)
    - Card supports title prop rendering heading text (font-heading, 18px, 600)
  </acceptance_criteria>
  <action>
    Create three reusable UI components:

    **apps/web/src/components/ui/Button.tsx:**
    ```tsx
    import { cn } from '@/lib/utils';
    {`import { Loader2 } from 'lucide-react';`}

    interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
      variant?: 'primary' | 'secondary' | 'destructive' | 'ghost';
      loading?: boolean;
      loadingText?: string;
    }

    const variantStyles: Record<string, string> = {
      primary: 'bg-primary text-white hover:bg-primary-active focus-visible:ring-primary',
      secondary: 'bg-surface-card text-ink border border-hairline-strong hover:bg-canvas-soft focus-visible:ring-primary',
      destructive: 'bg-semantic-error text-white hover:bg-red-700 focus-visible:ring-semantic-error',
      ghost: 'bg-transparent text-ink hover:bg-canvas-soft focus-visible:ring-primary',
    };

    export function Button({
      variant = 'primary',
      loading = false,
      loadingText,
      children,
      className,
      disabled,
      ...props
    }: ButtonProps) {
      return (
        <button
          className={cn(
            'inline-flex items-center justify-center gap-xs rounded-md font-medium text-sm focus-ring transition-colors',
            'min-h-[44px] px-[18px] h-10',
            variantStyles[variant],
            (disabled || loading) && 'opacity-50 cursor-not-allowed',
            className
          )}
          disabled={disabled || loading}
          {...props}
        >
          {loading && <Loader2 className="w-4 h-4 animate-spin" />}
          {loading ? loadingText || children : children}
        </button>
      );
    }
    ```

    **apps/web/src/components/ui/Input.tsx:**
    ```tsx
    import { cn } from '@/lib/utils';
    {`import { Eye, EyeOff } from 'lucide-react';`}
    import { useState } from 'react';

    interface InputProps extends React.InputHTMLAttributes<HTMLInputElement> {
      label?: string;
      error?: string;
    }

    export function Input({ label, error, className, type, ...props }: InputProps) {
      const [showPassword, setShowPassword] = useState(false);
      const isPassword = type === 'password';
      const inputType = isPassword && showPassword ? 'text' : type;

      return (
        <div className="space-y-xs">
          {label && (
            <label className="block text-sm text-body">{label}</label>
          )}
          <div className="relative">
            <input
              type={inputType}
              className={cn(
                'w-full bg-surface-card text-ink border rounded-md text-body focus-ring transition-colors',
                'h-11 px-4',
                error ? 'border-semantic-error' : 'border-hairline focus:border-primary',
                className
              )}
              {...props}
            />
            {isPassword && (
              <button
                type="button"
                onClick={() => setShowPassword(!showPassword)}
                className="absolute right-3 top-1/2 -translate-y-1/2 text-muted hover:text-ink focus-ring"
                aria-label={showPassword ? 'Hide password' : 'Show password'}
              >
                {showPassword ? <EyeOff className="w-4 h-4" /> : <Eye className="w-4 h-4" />}
              </button>
            )}
          </div>
          {error && (
            <p className="text-small text-semantic-error" role="alert">{error}</p>
          )}
        </div>
      );
    }
    ```

    **apps/web/src/components/ui/Card.tsx:**
    ```tsx
    import { cn } from '@/lib/utils';

    interface CardProps {
      children: React.ReactNode;
      title?: string;
      className?: string;
    }

    export function Card({ children, title, className }: CardProps) {
      return (
        <div className={cn('bg-surface-card rounded-lg border border-hairline p-md', className)}>
          {title && (
            <h3 className="font-heading text-[18px] font-semibold text-ink mb-md">{title}</h3>
          )}
          {children}
        </div>
      );
    }
    ```

    **Create apps/web/src/lib/utils.ts:**
    ```typescript
    import { type ClassValue, clsx } from 'clsx';
    import { twMerge } from 'tailwind-merge';

    export function cn(...inputs: ClassValue[]) {
      return twMerge(clsx(inputs));
    }
    ```
  </action>
  <verify>
    <automated>test -f apps/web/src/components/ui/Button.tsx && test -f apps/web/src/components/ui/Input.tsx && test -f apps/web/src/components/ui/Card.tsx && test -f apps/web/src/lib/utils.ts && grep -q "bg-primary" apps/web/src/components/ui/Button.tsx && grep -q "bg-surface-card" apps/web/src/components/ui/Input.tsx && grep -q "bg-surface-card" apps/web/src/components/ui/Card.tsx && echo "All UI components exist with Cursor brand colors" || echo "MISSING UI components"</automated>
  </verify>
  <done>Button (primary/secondary/destructive/ghost variants with loading state, 44px min-height), Input (with password show/hide toggle, error state, label), Card (with optional title), and cn() utility created with Cursor brand colors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Next.js client → Proxy API (localhost:3456) | Trusted: localhost-only, no network exposure |
| Tauri WebView → Next.js | Trusted: same-origin within Tauri app bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | Password input show/hide | mitigate | Password field masked by default, show/hide toggle requires explicit user action, never logs key values |
| T-03-06 | Tampering | Client-side navigation | accept | Next.js client-side routing, no sensitive data in URL parameters |
</threat_model>

<verification>
- Next.js dev server starts: `cd apps/web && npm run dev` serves at localhost:3000
- Sidebar renders with 4 nav items visible
- Clicking nav items changes URL and active state (orange left border)
- Cursor brand colors visible: cream background, white sidebar, orange active nav indicator
- Button component renders with primary variant showing #f54e00 background
- Input component renders with password toggle working
- Card component renders with white background and hairline border
</verification>

<success_criteria>
- Next.js 15 app at apps/web/ with Tailwind CSS fully configured with Cursor brand tokens
- AppShell layout renders: 240px sidebar (white) + content area (cream #f7f7f4)
- Sidebar has header with "ClaudeCode Proxy" + status dot placeholder
- SidebarNav has 4 items: Status, Providers, Model Mapping, Settings with Lucide icons
- Active nav item shows orange left border (#f54e00) and cream-soft background
- Button, Input, Card components created with Cursor brand styling
- All 4 route pages exist as placeholders (/providers, /mapping, /settings, /)
- No shadcn used — all manual Tailwind components
</success_criteria>

<output>
After completion, create `.planning/phases/03-desktop-ui-shell/03-02-SUMMARY.md`
</output>

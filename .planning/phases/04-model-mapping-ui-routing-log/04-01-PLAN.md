---
phase: "04"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/proxy/src/types/index.ts
  - packages/proxy/src/services/requestLog.ts
  - packages/proxy/src/middleware/requestLogger.ts
  - packages/proxy/src/index.ts
  - packages/proxy/src/routes/admin.ts
  - packages/proxy/tests/services/requestLog.test.ts
  - packages/proxy/tests/middleware/requestLogger.test.ts
autonomous: true
requirements:
  - UI-06
user_setup: []

must_haves:
  truths:
    - "Every POST /v1/messages request is logged with timestamp, model, status, and duration"
    - "Log file at ~/.claude-code-proxy/request-log.json persists across proxy restarts"
    - "Ring buffer maintains exactly 50 most recent entries — oldest dropped when 51st arrives"
  artifacts:
    - path: "packages/proxy/src/types/index.ts"
      provides: "RequestLogEntry interface with all required fields"
      contains: "interface RequestLogEntry"
    - path: "packages/proxy/src/services/requestLog.ts"
      provides: "RequestLogService class with ring buffer, atomic writes, load/persist"
      exports: ["RequestLogService", "requestLogService"]
    - path: "packages/proxy/src/middleware/requestLogger.ts"
      provides: "Express middleware using on-finished for SSE-safe lifecycle hooks"
      exports: ["requestLoggerMiddleware"]
    - path: "packages/proxy/src/index.ts"
      provides: "Middleware wired before handleProxyRequest, requestLogService.load() in startup"
      contains: "requestLoggerMiddleware"
    - path: "packages/proxy/src/routes/admin.ts"
      provides: "GET /admin/logs endpoint returning all log entries"
      contains: "/logs"
  key_links:
    - from: "packages/proxy/src/middleware/requestLogger.ts"
      to: "packages/proxy/src/services/requestLog.ts"
      via: "requestLogService.addEntry() called in on-finished callback"
      pattern: "requestLogService\\.addEntry"
    - from: "packages/proxy/src/index.ts"
      to: "packages/proxy/src/middleware/requestLogger.ts"
      via: "middleware inserted before handleProxyRequest on /v1/messages route"
      pattern: "requestLoggerMiddleware"
    - from: "packages/proxy/src/routes/admin.ts"
      to: "packages/proxy/src/services/requestLog.ts"
      via: "requestLogService.getAll() in GET /admin/logs handler"
      pattern: "requestLogService\\.getAll"
---

<objective>
Implement request logging backend: Express middleware that captures request metadata for every /v1/messages POST, a JSON file ring buffer service persisting the last 50 entries at ~/.claude-code-proxy/request-log.json, and a GET /admin/logs endpoint.

Purpose: Provide observability into request routing — which models are being used, which providers handle them, success/error rates, and response durations. This is the data foundation for the Routing Log UI (Plan 03).

Output: RequestLogService, requestLoggerMiddleware, RequestLogEntry type, /admin/logs endpoint, unit tests.
</objective>

<execution_context>
@/Users/davidefeltrin/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/davidefeltrin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-model-mapping-ui-routing-log/04-CONTEXT.md
@.planning/phases/04-model-mapping-ui-routing-log/04-RESEARCH.md
@.planning/phases/04-model-mapping-ui-routing-log/04-PATTERNS.md

@packages/proxy/src/services/config.ts — Atomic write pattern (temp file + renameSync), directory creation, file permissions
@packages/proxy/src/types/index.ts — Existing type definitions (ClaudeTier, LLMProvider, ModelRoute) — extend with RequestLogEntry
@packages/proxy/src/index.ts — Express entry point, line 122: app.post('/v1/messages', express.json(), handleProxyRequest) — insert middleware here
@packages/proxy/src/routes/admin.ts — Existing admin API pattern (error handling, JSON responses) — add GET /admin/logs
@packages/proxy/src/proxy.ts — Proxy handler that resolves routes; will set req._logContext for middleware enrichment
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install on-finished + create RequestLogEntry type + RequestLogService</name>
  <files>packages/proxy/src/types/index.ts, packages/proxy/src/services/requestLog.ts</files>
  <read_first>
    - packages/proxy/src/services/config.ts — Copy atomic write pattern: mkdirSync with 0o700, tempPath + writeFileSync with 0o600, renameSync
    - packages/proxy/src/types/index.ts — Current type definitions; add RequestLogEntry interface after RouteResolution (line 41)
  </read_first>
  <action>
1. Install on-finished dependency:
   cd packages/proxy && npm install on-finished

2. Add RequestLogEntry interface to packages/proxy/src/types/index.ts after the RouteResolution interface (after line 41):

```typescript
export interface RequestLogEntry {
  timestamp: string;
  requestModel: string;
  claudeTier?: ClaudeTier;
  providerName?: string;
  targetModel?: string;
  status: 'success' | 'error';
  durationMs: number;
  statusCode: number;
  requestBodyPreview?: string;   // truncated to ~2KB
  responsePreview?: string;      // truncated preview
}
```

3. Create packages/proxy/src/services/requestLog.ts with RequestLogService class following ConfigService pattern exactly:

- Import: `import { readFileSync, writeFileSync, existsSync, mkdirSync, renameSync } from 'fs';`
- Import: `import { join } from 'path';`
- Import: `import os from 'os';`
- Import: `import type { RequestLogEntry, ClaudeTier } from '../types/index.js';`
- Constants:
  - `const LOG_DIR = join(os.homedir(), '.claude-code-proxy');`
  - `const LOG_FILE = join(LOG_DIR, 'request-log.json');`
  - `const MAX_ENTRIES = 50;`
  - `const BODY_TRUNCATE_LIMIT = 2048;` (2KB truncation limit per D-48)
- Class RequestLogService:
  - Constructor accepts optional `logFile?: string` parameter for testability (same pattern as ConfigService). Default to LOG_FILE.
  - Private `entries: RequestLogEntry[] = []`
  - Private `logFile: string`
  - `load(): RequestLogEntry[]` — try/catch, if file doesn't exist return [], otherwise JSON.parse and return entries
  - `addEntry(entry: RequestLogEntry): void` — push to entries array, if length > MAX_ENTRIES use `this.entries = this.entries.slice(-MAX_ENTRIES)`, then call `this.persist()`
  - `getAll(): RequestLogEntry[]` — return `[...this.entries]` (copy, not reference)
  - `enrichLastEntry(update: Partial<RequestLogEntry>): void` — if entries.length > 0, merge update into last entry, then persist. Used by proxy handler to add claudeTier/providerName/targetModel after route resolution.
  - `truncateBody(body: unknown): string` — JSON.stringify body, if length > BODY_TRUNCATE_LIMIT return first BODY_TRUNCATE_LIMIT chars + '...', else return full string
  - `private persist(): void` — ensure LOG_DIR exists with mkdirSync recursive 0o700, write to tempPath with writeFileSync 0o600, renameSync to final path
- Export singleton: `export const requestLogService = new RequestLogService();`

The persist method must use renameSync (not require('fs').renameSync) — import it directly from 'fs' for cleaner code.
  </action>
  <verify>
    <automated>cd packages/proxy && npx tsc --noEmit src/services/requestLog.ts src/types/index.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/proxy/src/types/index.ts contains "interface RequestLogEntry" with fields: timestamp, requestModel, claudeTier, providerName, targetModel, status, durationMs, statusCode, requestBodyPreview, responsePreview
    - packages/proxy/src/services/requestLog.ts contains "class RequestLogService" with methods: load, addEntry, getAll, enrichLastEntry, persist
    - packages/proxy/src/services/requestLog.ts contains "const MAX_ENTRIES = 50"
    - packages/proxy/src/services/requestLog.ts contains "const BODY_TRUNCATE_LIMIT = 2048"
    - packages/proxy/src/services/requestLog.ts imports renameSync directly from 'fs' (not via require)
    - packages/proxy/src/services/requestLog.ts exports singleton "requestLogService"
    - on-finished package installed in packages/proxy/node_modules/on-finished
  </acceptance_criteria>
  <done>RequestLogEntry type added, RequestLogService class created with ring buffer logic, atomic writes, truncation, and enrichment method. on-finstalled.</done>
</task>

<task type="auto">
  <name>Task 2: Create Express logging middleware + wire in index.ts</name>
  <files>packages/proxy/src/middleware/requestLogger.ts, packages/proxy/src/index.ts, packages/proxy/src/proxy.ts</files>
  <read_first>
    - packages/proxy/src/index.ts — Line 122: app.post('/v1/messages', express.json(), handleProxyRequest) — insert middleware here
    - packages/proxy/src/proxy.ts — handleProxyRequest function; add req._logContext enrichment after route resolution (line 67 after resolution check)
    - packages/proxy/src/middleware/requestLogger.ts — File does not exist yet; create new
  </read_first>
  <action>
1. Create packages/proxy/src/middleware/requestLogger.ts:

- Import: `import onFinished from 'on-finished';`
- Import: `import type { Request, Response, NextFunction } from 'express';`
- Import: `import { requestLogService } from '../services/requestLog.js';`
- Export function `requestLoggerMiddleware(req: Request, res: Response, next: NextFunction): void`:
  - Guard: if `req.path !== '/v1/messages' || req.method !== 'POST'`, call `return next()`
  - Capture: `const startTime = Date.now()`
  - Capture: `const requestModel = (req.body as any)?.model || 'unknown'`
  - Capture: `const requestBodyPreview = requestLogService.truncateBody(req.body)`
  - Set up on-finished callback:
    ```typescript
    onFinished(res, (err) => {
      const durationMs = Date.now() - startTime;
      const status = err ? 'error' : (res.statusCode >= 400 ? 'error' : 'success');
      
      // Read enrichment data set by proxy handler
      const logContext = (req as any)._logContext || {};
      
      requestLogService.addEntry({
        timestamp: new Date().toISOString(),
        requestModel,
        claudeTier: logContext.claudeTier,
        providerName: logContext.providerName,
        targetModel: logContext.targetModel,
        status,
        durationMs,
        statusCode: res.statusCode,
        requestBodyPreview,
      });
    });
    ```
  - Call `next()`

2. Wire middleware in packages/proxy/src/index.ts:
   - Add import at top: `import { requestLoggerMiddleware } from './middleware/requestLogger.js';`
   - Add import at top: `import { requestLogService } from './services/requestLog.js';`
   - Change line 122 from:
     `app.post('/v1/messages', express.json(), handleProxyRequest);`
     To:
     `app.post('/v1/messages', express.json(), requestLoggerMiddleware, handleProxyRequest);`

3. Add enrichment in packages/proxy/src/proxy.ts:
   - After line 67 (after the `if (!resolution)` check passes, before line 70 `const apiKey = await...`), add:
     ```typescript
     // Enrich request log with route resolution data (per D-45)
     (req as any)._logContext = {
       claudeTier: resolution.claudeTier,
       providerName: resolution.provider.name,
       targetModel: resolution.targetModel,
     };
     ```
   - This sets the context on the request object BEFORE the response is sent, so the middleware's on-finished callback can read it.

4. Load request log on startup in packages/proxy/src/index.ts:
   - In the `loadConfigOnStartup()` function, after line 140 (after the console.log about loaded providers/routes), add:
     `requestLogService.load();`
   - Add console.log: `console.log('[Proxy] Request log loaded from ~/.claude-code-proxy/request-log.json');`
  </action>
  <verify>
    <automated>cd packages/proxy && npx tsc --noEmit src/middleware/requestLogger.ts src/index.ts src/proxy.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/proxy/src/middleware/requestLogger.ts contains "export function requestLoggerMiddleware"
    - packages/proxy/src/middleware/requestLogger.ts imports "onFinished from 'on-finished'"
    - packages/proxy/src/middleware/requestLogger.ts calls "requestLogService.addEntry" inside onFinished callback
    - packages/proxy/src/index.ts line with app.post('/v1/messages') contains "requestLoggerMiddleware" before "handleProxyRequest"
    - packages/proxy/src/index.ts loadConfigOnStartup function contains "requestLogService.load()"
    - packages/proxy/src/proxy.ts contains "(req as any)._logContext" assignment after resolution check
    - packages/proxy/src/proxy.ts _logContext includes claudeTier, providerName, targetModel
  </acceptance_criteria>
  <done>Middleware captures request metadata via on-finished, proxy handler enriches with route data, middleware wired before handleProxyRequest, log loaded on startup.</done>
</task>

<task type="auto">
  <name>Task 3: Add GET /admin/logs endpoint + ring buffer + middleware tests</name>
  <files>packages/proxy/src/routes/admin.ts, packages/proxy/tests/services/requestLog.test.ts, packages/proxy/tests/middleware/requestLogger.test.ts</files>
  <read_first>
    - packages/proxy/src/routes/admin.ts — Existing route pattern: try/catch, console.error('[Admin] Error...'), res.status(500).json({ error: '...' })
    - packages/proxy/vitest.config.ts — Test config: include: ['tests/**/*.test.ts']
  </read_first>
  <action>
1. Add GET /admin/logs endpoint to packages/proxy/src/routes/admin.ts:
   - Add import: `import { requestLogService } from '../services/requestLog.js';`
   - Add route BEFORE the `export default router;` line:
     ```typescript
     /**
      * GET /admin/logs
      * Return all request log entries (max 50, ring buffer)
      */
     router.get('/logs', (req, res) => {
       try {
         const entries = requestLogService.getAll();
         res.json(entries);
       } catch (error) {
         console.error('[Admin] Error loading request logs:', error);
         res.status(500).json({ error: 'Failed to load request logs' });
       }
     });
     ```

2. Create packages/proxy/tests/services/requestLog.test.ts with vitest tests:
   - Import: `import { describe, it, expect, beforeEach, afterEach } from 'vitest';`
   - Import: `import { RequestLogService } from '../../src/services/requestLog.js';`
   - Import: `import { existsSync, readFileSync, rmSync } from 'fs';`
   - Import: `import { join } from 'path';`
   - Import: `import os from 'os';`
   - Test directory: `join(os.tmpdir(), 'claude-code-proxy-test')` (use tmpdir, not homedir, for tests)
   - Test file: `join(testDir, 'request-log-test.json')`
   - beforeEach: mkdirSync testDir if not exists, rmSync testFile if exists
   - afterEach: rmSync testFile if exists
   - Test 1: 'returns empty array when file does not exist' — new RequestLogService(testFile), expect(service.getAll()).toEqual([])
   - Test 2: 'adds entry and persists to file' — addEntry with test entry, expect(getAll()).toHaveLength(1), expect(existsSync(testFile)).toBe(true), read file and verify JSON contains entry
   - Test 3: 'drops oldest entry when exceeding 50 entries (ring buffer)' — add 51 entries, expect(getAll()).toHaveLength(50), expect first entry timestamp is the 2nd added (not the 1st)
   - Test 4: 'uses atomic write pattern (temp file + rename)' — addEntry, expect existsSync(`${testFile}.tmp`) to be false (temp file cleaned up), expect existsSync(testFile) to be true
   - Test 5: 'enrichLastEntry updates the most recent entry' — addEntry, enrichLastEntry with { providerName: 'test' }), expect(getAll()[0].providerName).toBe('test')
   - Test 6: 'truncateBody limits output to 2KB' — create object with 5KB string, expect truncateBody result length <= 2048 + 3 (for '...')

3. Create packages/proxy/tests/middleware/requestLogger.test.ts:
   - Import: `import { describe, it, expect, vi, beforeEach } from 'vitest';`
   - Import: `import { requestLoggerMiddleware } from '../../src/middleware/requestLogger.js';`
   - Mock requestLogService: `vi.mock('../../src/services/requestLog.js', () => ({ requestLogService: { addEntry: vi.fn(), truncateBody: vi.fn((b) => JSON.stringify(b).slice(0, 100)) } }));`
   - Test 1: 'skips non /v1/messages requests' — create mock req with path='/health', method='GET', mock res, mock next, call middleware, expect next called, expect addEntry NOT called
   - Test 2: 'skips non-POST /v1/messages requests' — req with path='/v1/messages', method='GET', expect next called, addEntry NOT called
   - Test 3: 'logs POST /v1/messages requests on response finish' — req with path='/v1/messages', method='POST', body: { model: 'claude-sonnet-4' }, mock res with statusCode=200, use vi.fn() for onFinished callback simulation, expect addEntry called with status='success', requestModel='claude-sonnet-4'
   - Test 4: 'reads _logContext from request for enrichment' — req with _logContext: { claudeTier: 'sonnet', providerName: 'openrouter', targetModel: 'mimo-v2-flash' }, expect addEntry called with claudeTier='sonnet', providerName='openrouter'
  </action>
  <verify>
    <automated>cd packages/proxy && npm run test:run -- --run tests/services/requestLog.test.ts tests/middleware/requestLogger.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - packages/proxy/src/routes/admin.ts contains "router.get('/logs'"
    - packages/proxy/src/routes/admin.ts contains "requestLogService.getAll()"
    - packages/proxy/tests/services/requestLog.test.ts exists with at least 6 test cases (describe blocks)
    - packages/proxy/tests/services/requestLog.test.ts contains "describe('RequestLogService'"
    - packages/proxy/tests/services/requestLog.test.ts contains ring buffer test: "drops oldest entry when exceeding 50"
    - packages/proxy/tests/middleware/requestLogger.test.ts exists with at least 4 test cases
    - packages/proxy/tests/middleware/requestLogger.test.ts contains "requestLoggerMiddleware"
    - npm run test:run exits with code 0 for both test files
  </acceptance_criteria>
  <done>GET /admin/logs endpoint returns log entries, ring buffer tests pass (50-entry limit, atomic writes, enrichment), middleware tests pass (path filtering, logging, enrichment).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /v1/messages | Untrusted HTTP request body passes through logging middleware |
| RequestLogService → Disk | Log file written to user's home directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | requestLog.ts (file I/O) | mitigate | File permissions 0o600 on request-log.json (same as config.json), stored in user's ~/.claude-code-proxy/ directory |
| T-04-02 | Information Disclosure | requestLogger.ts (request body capture) | mitigate | Request body truncated to 2KB via truncateBody(); API keys in Authorization header not captured (middleware only reads req.body, not headers) |
| T-04-03 | Tampering | requestLog.ts (ring buffer) | accept | Single-user desktop app, log file integrity not security-critical; corruption results in data loss not code execution |
</threat_model>

<verification>
- npm run test:run --workspace=packages/proxy passes all existing tests plus new requestLog and requestLogger tests
- curl -X POST http://localhost:3456/v1/messages -H 'Content-Type: application/json' -d '{"model":"test"}' creates a log entry
- curl http://localhost:3456/admin/logs returns array with the log entry
- ~/.claude-code-proxy/request-log.json exists with valid JSON
</verification>

<success_criteria>
- RequestLogEntry type defined with all 10 fields (timestamp, requestModel, claudeTier, providerName, targetModel, status, durationMs, statusCode, requestBodyPreview, responsePreview)
- RequestLogService implements ring buffer: addEntry pushes, slice(-50) when > 50, atomic persist via temp+rename
- Middleware only logs POST /v1/messages, uses on-finished for SSE-safe lifecycle capture
- Proxy handler enriches log entries with claudeTier/providerName/targetModel via req._logContext
- GET /admin/logs returns all entries as JSON array
- All 10+ tests pass (6 RequestLogService + 4 middleware)
- Log file permissions 0o600, body truncated to 2KB
</success_criteria>

<output>
After completion, create `.planning/phases/04-model-mapping-ui-routing-log/04-01-SUMMARY.md`
</output>

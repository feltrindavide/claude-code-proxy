---
phase: "04"
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - packages/proxy/src/services/config.ts
  - packages/proxy/src/routes/admin.ts
  - packages/proxy/tests/services/config.exportImport.test.ts
  - packages/proxy/tests/routes/admin.exportImport.test.ts
autonomous: true
requirements:
  - MAP-04
user_setup: []

must_haves:
  truths:
    - "Export returns config with masked API keys — keyId replaced with '••••', no actual keys in output"
    - "Import validates incoming config against proxyConfigSchema before applying any changes"
    - "Auto-backup of current config created before import changes are applied"
    - "Import supports both 'merge' (add providers, replace routes) and 'replace' (full replacement) strategies"
  artifacts:
    - path: "packages/proxy/src/services/config.ts"
      provides: "exportConfig(), importConfig(), createBackup() methods on ConfigService"
      exports: ["exportConfig", "importConfig", "createBackup"]
    - path: "packages/proxy/src/routes/admin.ts"
      provides: "GET /admin/config/export, POST /admin/config/import, POST /admin/config/diff endpoints"
      contains: "/config/export"
  key_links:
    - from: "packages/proxy/src/routes/admin.ts"
      to: "packages/proxy/src/services/config.ts"
      via: "configService.exportConfig() for GET /config/export, configService.createBackup() + configService.importConfig() + configService.save() for POST /config/import"
      pattern: "configService\\.(exportConfig|importConfig|createBackup)"
    - from: "packages/proxy/src/routes/admin.ts"
      to: "packages/proxy/src/services/provider.ts"
      via: "providerService.reload() called after import to apply new config to running services"
      pattern: "providerService\\.reload"
---

<objective>
Extend ConfigService with export/import/backup methods and add three admin API endpoints: GET /admin/config/export, POST /admin/config/import, and POST /admin/config/diff. Export masks API keys per D-50. Import validates strictly against proxyConfigSchema per D-52, creates auto-backup per D-53, and supports merge/replace strategies per D-51.

Purpose: Enable configuration portability — users can back up their proxy config, share it across machines, or restore from a known-good state. The diff endpoint enables the frontend to show a visual preview before applying changes.

Output: ConfigService extensions, 3 new admin endpoints, unit tests for export/import/backup.
</objective>

<execution_context>
@/Users/davidefeltrin/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/davidefeltrin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-model-mapping-ui-routing-log/04-CONTEXT.md
@.planning/phases/04-model-mapping-ui-routing-log/04-RESEARCH.md
@.planning/phases/04-model-mapping-ui-routing-log/04-PATTERNS.md
@.planning/phases/04-model-mapping-ui-routing-log/04-01-PLAN.md — Plan 01 added /admin/logs endpoint to admin.ts; add new endpoints after it

@packages/proxy/src/services/config.ts — Existing ConfigService: load(), save(), getDefaults(), validateProvider(), validateRoute(). proxyConfigSchema zod schema at line 60. Atomic write pattern at lines 119-131.
@packages/proxy/src/routes/admin.ts — Existing admin routes. Plan 01 added GET /logs. Add export/import/diff endpoints after the /logs route.
@packages/proxy/src/services/provider.ts — providerService.reload(providers, routes) method for applying new config to running services.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exportConfig, importConfig, createBackup methods to ConfigService</name>
  <files>packages/proxy/src/services/config.ts</files>
  <read_first>
    - packages/proxy/src/services/config.ts — Full file. Note: proxyConfigSchema at line 60, save() atomic write at lines 112-132, load() at lines 86-106
    - packages/proxy/src/routes/admin.ts — Key masking pattern at lines 83-106 (GET /admin/providers): `keyMask: hasKey ? '••••' : null`
  </read_first>
  <action>
Add three methods to the ConfigService class in packages/proxy/src/services/config.ts. Insert them after the validateRoute method (after line 168) and before the singleton export (line 172).

1. exportConfig(): object
   - Calls this.load() to get current config
   - Masks all provider keyId values to '••••' (never expose actual keyId per D-50)
   - Returns object with exactly these three keys:
     ```typescript
     {
       providers: config.providers.map(p => ({ ...p, keyId: '••••' })),
       routes: config.routes,
       settings: { port: 3456 }
     }
     ```
   - Does NOT include: actual API keys, keyId values, request logs, runtime state

2. importConfig(data: unknown, strategy: 'merge' | 'replace'): AppConfig
   - Validates data against proxyConfigSchema: `const result = proxyConfigSchema.safeParse(data)`
   - If validation fails, throw Error with message: `Invalid config: ${result.error.errors.map(e => `${e.path.join('.')}: ${e.message}`).join('; ')}`
   - If strategy is 'replace': return result.data directly
   - If strategy is 'merge':
     - Load current config: `const current = this.load()`
     - Merge providers: combine current.providers with result.data.providers, deduplicating by name (incoming provider overwrites existing if same name)
     - Replace routes: use result.data.routes (incoming routes replace current)
     - Return merged AppConfig
   - Deduplication logic for merge: use a Map keyed by provider.name, add current providers first, then incoming (so incoming overwrites)

3. createBackup(): string (returns backup file path)
   - Load current config: `const config = this.load()`
   - Generate timestamp: `const timestamp = new Date().toISOString().replace(/[:.]/g, '-')`
   - Build backup path: `const backupPath = join(this.configDir, `config-backup-${timestamp}.json`)`
   - Write config to backupPath using writeFileSync with mode 0o600 (no atomic write needed for backup — it's append-only, not replacing)
   - Return backupPath

Import needed at top of file if not present:
- `import { join } from 'path';` (already imported at line 11)
- `import os from 'os';` (already imported at line 12)
</action>
  <verify>
    <automated>cd packages/proxy && npx tsc --noEmit src/services/config.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/proxy/src/services/config.ts ConfigService class contains method "exportConfig()"
    - exportConfig returns object with keys: "providers", "routes", "settings"
    - exportConfig maps provider keyId to '••••' (literal bullet character U+2022 repeated 4 times)
    - packages/proxy/src/services/config.ts ConfigService class contains method "importConfig(data: unknown, strategy:"
    - importConfig calls "proxyConfigSchema.safeParse(data)"
    - importConfig throws Error with "Invalid config:" prefix when validation fails
    - importConfig with strategy 'merge' uses Map for provider deduplication by name
    - packages/proxy/src/services/config.ts ConfigService class contains method "createBackup():"
    - createBackup writes file with name pattern "config-backup-" followed by ISO timestamp
    - createBackup returns the backup file path string
  </acceptance_criteria>
  <done>ConfigService has exportConfig (masked keys), importConfig (validate + merge/replace), createBackup (timestamped backup file).</done>
</task>

<task type="auto">
  <name>Task 2: Add /config/export, /config/import, /config/diff admin endpoints</name>
  <files>packages/proxy/src/routes/admin.ts</files>
  <read_first>
    - packages/proxy/src/routes/admin.ts — Full file. Note existing error pattern: try/catch, console.error('[Admin] Error...'), res.status(500).json({ error: '...' })
    - packages/proxy/src/services/config.ts — New methods from Task 1: exportConfig(), importConfig(), createBackup()
    - packages/proxy/src/services/provider.ts — providerService.reload(providers: LLMProvider[], routes: ModelRoute[]) method signature
  </read_first>
  <action>
Add three new endpoints to packages/proxy/src/routes/admin.ts. Insert them after the GET /logs endpoint (added by Plan 01) and before the `export default router;` line.

1. GET /admin/config/export:
   ```typescript
   router.get('/config/export', (req, res) => {
     try {
       const exported = configService.exportConfig();
       res.json(exported);
     } catch (error) {
       console.error('[Admin] Error exporting config:', error);
       res.status(500).json({ error: 'Failed to export config' });
     }
   });
   ```

2. POST /admin/config/import:
   ```typescript
   router.post('/config/import', (req, res) => {
     try {
       const { data, strategy } = req.body;
       if (!data || !strategy || !['merge', 'replace'].includes(strategy)) {
         return res.status(400).json({ error: 'data and strategy (merge|replace) are required' });
       }
       
       // Auto-backup current config before changes (D-53)
       const backupPath = configService.createBackup();
       console.log(`[Admin] Config backup created: ${backupPath}`);
       
       // Import with validation (D-52)
       const imported = configService.importConfig(data, strategy as 'merge' | 'replace');
       configService.save(imported);
       
        // Reload provider service with new config
        providerService.reload(imported.providers || [], imported.routes || []);
       
       res.json({ success: true, backupPath });
     } catch (error) {
       console.error('[Admin] Error importing config:', error);
       res.status(400).json({ error: error instanceof Error ? error.message : 'Failed to import config' });
     }
   });
    ```

3. POST /admin/config/diff:
   ```typescript
   router.post('/config/diff', (req, res) => {
     try {
       const { data } = req.body;
       if (!data) {
         return res.status(400).json({ error: 'data is required' });
       }
       const current = configService.exportConfig(); // Use exportConfig to get masked version
       res.json({ current, incoming: data });
     } catch (error) {
       console.error('[Admin] Error generating diff:', error);
       res.status(500).json({ error: 'Failed to generate diff' });
     }
   });
   ```

No new imports needed — configService and providerService are already imported.
  </action>
  <verify>
    <automated>cd packages/proxy && npx tsc --noEmit src/routes/admin.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/proxy/src/routes/admin.ts contains "router.get('/config/export'"
    - packages/proxy/src/routes/admin.ts contains "configService.exportConfig()"
    - packages/proxy/src/routes/admin.ts contains "router.post('/config/import'"
    - packages/proxy/src/routes/admin.ts contains "configService.createBackup()"
    - packages/proxy/src/routes/admin.ts contains "configService.importConfig(data, strategy"
    - packages/proxy/src/routes/admin.ts contains "providerService.reload(" (not configService.reload)
    - packages/proxy/src/routes/admin.ts contains "router.post('/config/diff'"
    - packages/proxy/src/routes/admin.ts import endpoint validates strategy is 'merge' or 'replace' with 400 response
  </acceptance_criteria>
  <done>Three admin endpoints added: GET /config/export (masked config), POST /config/import (validate + backup + apply + reload), POST /config/diff (current vs incoming for frontend diffing).</done>
</task>

<task type="auto">
  <name>Task 3: Unit tests for export/import/backup</name>
  <files>packages/proxy/tests/services/config.exportImport.test.ts, packages/proxy/tests/routes/admin.exportImport.test.ts</files>
  <read_first>
    - packages/proxy/vitest.config.ts — Test config: include: ['tests/**/*.test.ts']
    - packages/proxy/src/services/config.ts — proxyConfigSchema, ConfigService class, singleton export
  </read_first>
  <action>
1. Create packages/proxy/tests/services/config.exportImport.test.ts:
   - Import: `import { describe, it, expect, beforeEach, afterEach } from 'vitest';`
   - Import: `import { ConfigService } from '../../src/services/config.js';`
   - Import: `import { existsSync, rmSync, mkdirSync, readFileSync } from 'fs';`
   - Import: `import { join } from 'path';`
   - Import: `import os from 'os';`
   - Test directory: `join(os.tmpdir(), 'claude-code-proxy-export-test')`
   - Test file: `join(testDir, 'config-test.json')`
   - beforeEach: mkdirSync testDir recursive, rmSync testFile if exists
   - afterEach: clean up all files matching config-test*.json in testDir
   - Create test config helper: valid AppConfig with 1 provider and 1 route
   
   Tests:
   - Test 1: 'exportConfig masks all provider keyId values' — save test config, call exportConfig(), expect every provider.keyId === '••••'
   - Test 2: 'exportConfig includes providers, routes, and settings keys' — expect Object.keys(result).toEqual(['providers', 'routes', 'settings'])
   - Test 3: 'exportConfig settings includes port 3456' — expect result.settings.port === 3456
   - Test 4: 'importConfig with replace strategy returns validated data' — call importConfig(validConfig, 'replace'), expect result equals validConfig
   - Test 5: 'importConfig with merge strategy combines providers' — save config with provider 'A', importConfig with provider 'B' and strategy 'merge', expect result has both providers
   - Test 6: 'importConfig with merge strategy deduplicates providers by name (incoming wins)' — save config with provider 'A' (url1), importConfig with provider 'A' (url2) and strategy 'merge', expect provider 'A' has url2
   - Test 7: 'importConfig with merge strategy replaces routes entirely' — save config with route1, importConfig with route2 and strategy 'merge', expect result has only route2
   - Test 8: 'importConfig rejects invalid config with descriptive error' — call importConfig({ invalid: true }, 'replace'), expect error message contains "Invalid config:"
   - Test 9: 'importConfig rejects missing claudeTier in route' — call importConfig with route missing claudeTier, expect error mentions "claudeTier"
   - Test 10: 'createBackup writes timestamped file' — save config, call createBackup(), expect returned path contains "config-backup-", expect existsSync(returnedPath) === true
   - Test 11: 'createBackup file contains valid JSON config' — read backup file, JSON.parse, expect has providers and routes arrays

2. Create packages/proxy/tests/routes/admin.exportImport.test.ts:
   - Import: `import { describe, it, expect, vi, beforeEach } from 'vitest';`
   - Import express and supertest-like pattern (or use direct route testing)
   - Since the project may not have supertest installed, test the route handlers directly by mocking req/res:
   
   Tests (using mock req/res objects):
   - Test 1: 'GET /admin/config/export returns masked config' — mock req={}, mock res with json() spy, call the route handler, expect res.json called with object containing providers with keyId='••••'
   - Test 2: 'POST /admin/config/import rejects missing strategy' — mock req with body: { data: {} }, expect res.status(400) called
   - Test 3: 'POST /admin/config/import rejects invalid strategy' — mock req with body: { data: {}, strategy: 'delete' }, expect res.status(400) called
   - Test 4: 'POST /admin/config/import with valid data returns success' — mock req with body: { data: { providers: [], routes: [] }, strategy: 'replace' }, expect res.json called with { success: true }
   - Test 5: 'POST /admin/config/diff returns current and incoming' — mock req with body: { data: { providers: [], routes: [] } }, expect res.json called with { current: ..., incoming: ... }
  </action>
  <verify>
    <automated>cd packages/proxy && npm run test:run -- --run tests/services/config.exportImport.test.ts tests/routes/admin.exportImport.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - packages/proxy/tests/services/config.exportImport.test.ts exists with at least 11 test cases
    - packages/proxy/tests/services/config.exportImport.test.ts contains "describe('ConfigService export/import'"
    - packages/proxy/tests/services/config.exportImport.test.ts contains test for key masking: "keyId" and "'••••'"
    - packages/proxy/tests/services/config.exportImport.test.ts contains test for merge deduplication: "deduplicates providers by name"
    - packages/proxy/tests/services/config.exportImport.test.ts contains test for createBackup: "createBackup writes timestamped file"
    - packages/proxy/tests/routes/admin.exportImport.test.ts exists with at least 5 test cases
    - packages/proxy/tests/routes/admin.exportImport.test.ts contains "describe('Admin export/import routes'"
    - npm run test:run exits with code 0 for both test files
  </acceptance_criteria>
  <done>16 tests pass: exportConfig masks keys, importConfig validates/merges/replaces, createBackup writes timestamped file, admin endpoints return correct responses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Import endpoint → ConfigService | Untrusted JSON data from client validated before application |
| Export endpoint → Client | Config data with masked keys sent to localhost client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | config.ts exportConfig() | mitigate | All provider keyId values replaced with '••••' literal; actual API keys never stored in config (Keychain-only per D-14); export output contains only providers (masked), routes, settings |
| T-04-05 | Tampering | config.ts importConfig() | mitigate | Strict zod validation via proxyConfigSchema before any config changes applied; invalid data throws Error with specific field errors (D-52); merge strategy deduplicates by provider name to prevent duplicate entries |
| T-04-06 | Repudiation | config.ts createBackup() | mitigate | Auto-backup created before every import with ISO timestamp in filename; backup file written with 0o600 permissions; backup path logged to console for audit trail |
| T-04-07 | Tampering | admin.ts POST /config/import | mitigate | Strategy parameter validated against whitelist ['merge', 'replace']; non-whitelisted values return 400; providerService.reload() only called after successful save |
</threat_model>

<verification>
- npm run test:run --workspace=packages/proxy passes all existing tests plus new config.exportImport and admin.exportImport tests
- curl http://localhost:3456/admin/config/export returns JSON with providers[].keyId === '••••'
- curl -X POST http://localhost:3456/admin/config/import -H 'Content-Type: application/json' -d '{"data":{"providers":[],"routes":[]},"strategy":"replace"}' returns {"success":true}
- ~/.claude-code-proxy/config-backup-*.json created after import
- curl -X POST http://localhost:3456/admin/config/import -H 'Content-Type: application/json' -d '{"data":{"invalid":true},"strategy":"replace"}' returns 400 with error message
</verification>

<success_criteria>
- ConfigService.exportConfig() returns { providers: [...masked], routes: [...], settings: { port: 3456 } } with all keyId = '••••'
- ConfigService.importConfig() validates against proxyConfigSchema, throws descriptive error on failure
- ConfigService.importConfig(data, 'merge') combines providers (dedup by name, incoming wins), replaces routes
- ConfigService.importConfig(data, 'replace') returns validated data directly
- ConfigService.createBackup() writes timestamped JSON file to ~/.claude-code-proxy/, returns path
- GET /admin/config/export returns masked config
- POST /admin/config/import validates, backs up, saves, reloads providers
- POST /admin/config/diff returns { current, incoming } for frontend diffing
- 16+ tests pass (11 ConfigService + 5 admin routes)
</success_criteria>

<output>
After completion, create `.planning/phases/04-model-mapping-ui-routing-log/04-02-SUMMARY.md`
</output>

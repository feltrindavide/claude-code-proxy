---
phase: "06"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/playwright.config.ts
  - e2e/fixtures.ts
  - e2e/pages/StatusPage.ts
  - e2e/pages/ProviderForm.ts
  - e2e/pages/ModelMappingPage.ts
  - e2e/pages/RoutingLogPage.ts
  - e2e/pages/SettingsPage.ts
  - e2e/tests/01-happy-path.spec.ts
  - e2e/tests/02-provider-types.spec.ts
  - e2e/tests/03-edge-cases.spec.ts
  - e2e/tests/04-config-export.spec.ts
  - e2e/utils/test-helpers.ts
  - package.json
autonomous: true
requirements:
  - SC-06-01
  - D-74
  - D-75
  - D-76
must_haves:
  truths:
    - "E2E tests can be run with npx playwright test"
    - "Tests verify proxy routing with each provider type (OpenRouter, OpenCode, Ollama, Custom)"
    - "Tests cover happy path, edge cases (provider unavailable, rate limiting, retry), and config export/import"
    - "Page Object Models encapsulate UI interactions for all 5 pages"
  artifacts:
    - path: "e2e/playwright.config.ts"
      provides: "Playwright configuration with webServer for Express proxy + Next.js"
      exports: ["default config"]
    - path: "e2e/fixtures.ts"
      provides: "Global setup/teardown with isolated test config state"
      exports: ["globalSetup", "globalTeardown"]
    - path: "e2e/pages/*.ts"
      provides: "Page Object Models for StatusPage, ProviderForm, ModelMappingPage, RoutingLogPage, SettingsPage"
      exports: ["StatusPagePage", "ProviderFormPage", "ModelMappingPage", "RoutingLogPage", "SettingsPage"]
    - path: "e2e/tests/*.spec.ts"
      provides: "E2E test files covering all flows"
      exports: ["test.describe blocks"]
    - path: "e2e/utils/test-helpers.ts"
      provides: "Shared test utilities (cleanTestConfig, pollHealthEndpoint)"
      exports: ["cleanTestConfig", "pollHealthEndpoint", "TEST_CONFIG_DIR"]
  key_links:
    - from: "e2e/playwright.config.ts"
      to: "packages/proxy/src/index.ts"
      via: "webServer command starts Express proxy"
      pattern: "cd packages/proxy && npx tsx src/index"
    - from: "e2e/tests/*.spec.ts"
      to: "e2e/pages/*.ts"
      via: "import Page Object classes"
      pattern: "from '../pages/"
---

<objective>
Set up Playwright E2E testing infrastructure with Page Object Models and test files covering all provider types, happy path, edge cases, and config export/import flows.

Purpose: Verify the full stack (Next.js UI + Express proxy) works correctly with each provider type, per D-74, D-75, D-76.
Output: Complete E2E test suite in e2e/ directory with 4 test files and 5 page objects.
</objective>

<execution_context>
@/Users/davidefeltrin/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/davidefeltrin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-documentation/06-CONTEXT.md
@.planning/phases/06-testing-documentation/06-RESEARCH.md
@.planning/phases/06-testing-documentation/06-PATTERNS.md
@.planning/phases/06-testing-documentation/06-VALIDATION.md
@packages/proxy/vitest.config.ts
@packages/proxy/tests/routes/admin.test.ts
@packages/proxy/tests/services/provider.test.ts
@packages/proxy/tests/services/rateLimiter.test.ts
@packages/proxy/tests/services/config.exportImport.test.ts
@apps/web/src/components/StatusPage.tsx
@apps/web/src/components/ProviderForm.tsx
@apps/web/src/components/ModelMappingForm.tsx
@apps/web/src/components/RoutingLogTable.tsx
@apps/web/src/components/SettingsForm.tsx
@apps/web/src/components/ConfigExportImport.tsx
@package.json

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/proxy/src/services/config.ts:
```typescript
// Config schema uses zod validation
// Provider: { name, baseUrl, keyId, models[], enabled, priority }
// Route: { claudeTier: 'opus' | 'sonnet' | 'haiku', providerName, targetModel }
```

From apps/web/src/stores/healthStore.ts:
```typescript
// Health check results: { providerName, status: 'healthy' | 'unhealthy' | 'unknown', latency, error }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Playwright and create test infrastructure</name>
  <files>e2e/playwright.config.ts, e2e/fixtures.ts, e2e/utils/test-helpers.ts, package.json</files>
  <read_first>
    - package.json (current scripts and dependencies)
    - packages/proxy/vitest.config.ts (existing test config pattern)
    - packages/proxy/tests/routes/admin.test.ts (existing test setup pattern)
    - .planning/phases/06-testing-documentation/06-RESEARCH.md (Playwright config example, pitfalls)
  </read_first>
  <action>
1. Install Playwright dependencies at workspace root:
   ```bash
   npm install -D @playwright/test playwright
   npx playwright install chromium
   ```

2. Create `e2e/utils/test-helpers.ts` with these exact exports:
   - `TEST_CONFIG_DIR` constant set to `'/tmp/claude-proxy-e2e-test'`
   - `cleanTestConfig()` function: removes TEST_CONFIG_DIR if exists, recreates it with `mkdirSync(TEST_CONFIG_DIR, { recursive: true, mode: 0o700 })`
   - `pollHealthEndpoint(url: string, timeout?: number)` function: polls URL with fetch, retries every 500ms until 200 response or timeout (default 30000ms), throws Error if timeout
   - `getTestProvider(name: string)` function: returns `{ name, baseUrl: 'https://api.test.com/v1', apiKey: 'test-key-12345' }`

3. Create `e2e/fixtures.ts` with globalSetup and globalTeardown:
   - `globalSetup()`: sets `process.env.CONFIG_DIR = TEST_CONFIG_DIR`, calls `cleanTestConfig()`, polls `http://localhost:3456/health` using pollHealthEndpoint
   - `globalTeardown()`: removes TEST_CONFIG_DIR recursively, logs cleanup completion

4. Create `e2e/playwright.config.ts` with this exact configuration:
   - `testDir: './tests'`
   - `fullyParallel: false` (E2E tests share proxy state)
   - `timeout: 60_000`
   - `expect: { timeout: 10_000 }`
   - `reporter: [['html', { open: 'never' }], ['list']]`
   - `use: { baseURL: 'http://localhost:3000', trace: 'on-first-retry', screenshot: 'only-on-failure', video: 'retain-on-failure' }`
   - `projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }]`
   - `globalSetup` and `globalTeardown` pointing to `./fixtures.ts`
   - `webServer` array with two entries:
     - Express proxy: `command: 'cd packages/proxy && npx tsx src/index.ts'`, `url: 'http://localhost:3456/health'`, `timeout: 30_000`, `reuseExistingServer: !process.env.CI`
     - Next.js dev server: `command: 'npm run dev --workspace=apps/web'`, `url: 'http://localhost:3000'`, `timeout: 60_000`, `reuseExistingServer: !process.env.CI`

5. Add to root `package.json` scripts:
   - `"test:e2e": "npx playwright test --project=chromium"`
   - `"test:e2e:smoke": "npx playwright test --project=chromium --grep \"@smoke\""`
   - `"test:e2e:ui": "npx playwright test --project=chromium --ui"`
  </action>
  <verify>
    <automated>npx playwright test --list 2>&1 | head -5</automated>
  </verify>
  <done>
    - package.json contains test:e2e, test:e2e:smoke, test:e2e:ui scripts
    - e2e/playwright.config.ts exists with webServer config for both proxy (port 3456) and Next.js (port 3000)
    - e2e/fixtures.ts exports globalSetup and globalTeardown functions
    - e2e/utils/test-helpers.ts exports TEST_CONFIG_DIR, cleanTestConfig, pollHealthEndpoint, getTestProvider
    - @playwright/test and playwright are in devDependencies of root package.json
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Page Object Models for all 5 UI pages</name>
  <files>e2e/pages/StatusPage.ts, e2e/pages/ProviderForm.ts, e2e/pages/ModelMappingPage.ts, e2e/pages/RoutingLogPage.ts, e2e/pages/SettingsPage.ts</files>
  <read_first>
    - apps/web/src/components/StatusPage.tsx (component structure for selectors)
    - apps/web/src/components/ProviderForm.tsx (form fields: name, baseUrl, apiKey, type, enabled, priority)
    - apps/web/src/components/ModelMappingForm.tsx (mapping table structure)
    - apps/web/src/components/RoutingLogTable.tsx (log table structure)
    - apps/web/src/components/SettingsForm.tsx (settings form)
    - apps/web/src/components/ConfigExportImport.tsx (export/import buttons and file input)
    - .planning/phases/06-testing-documentation/06-PATTERNS.md (Page Object patterns for each page)
  </read_first>
  <action>
Create 5 Page Object Model files in e2e/pages/. Each file must:
- Import `{ expect, type Locator, type Page }` from `'@playwright/test'`
- Export a class with constructor(page: Page), readonly page, and Locators for key elements
- Use `getByRole`, `getByLabel`, `getByText` selectors (NOT CSS selectors)

1. `e2e/pages/StatusPage.ts` — class `StatusPagePage`:
   - Locators: `healthCard` (getByRole('region', { name: /health/i })), `providerStatusList` (getByRole('list', { name: /providers/i }))
   - Methods: `async goto()` → page.goto('/'), `async waitForStatusReady()` → expect(this.healthCard).toBeVisible()

2. `e2e/pages/ProviderForm.ts` — class `ProviderFormPage`:
   - Locators: `nameInput` (getByLabel('Name')), `baseUrlInput` (getByLabel('Base URL')), `apiKeyInput` (getByLabel('API Key')), `providerTypeSelect` (getByRole('combobox', { name: /provider type/i })), `testButton` (getByRole('button', { name: 'Test Connection' })), `saveButton` (getByRole('button', { name: 'Save Provider' })), `cancelButton` (getByRole('button', { name: 'Cancel' }))
   - Methods: `async fillProvider(name, baseUrl, apiKey, type?)` fills all 4 fields, `async testConnection()` clicks test button, `async save()` clicks save button, `async cancel()` clicks cancel button

3. `e2e/pages/ModelMappingPage.ts` — class `ModelMappingPage`:
   - Locators: `mappingTable` (getByRole('table', { name: /model mappings/i })), `saveButton` (getByRole('button', { name: /save.*mapping/i }))
   - Methods: `async goto()` → page.goto('/mapping'), `async updateMapping(tier, provider, model)` finds row by tier text, updates provider and model inputs in that row

4. `e2e/pages/RoutingLogPage.ts` — class `RoutingLogPage`:
   - Locators: `logTable` (getByRole('table', { name: /routing log/i })), `clearButton` (getByRole('button', { name: /clear log/i }))
   - Methods: `async goto()` → page.goto('/log'), `async waitForLogEntry(model)` → expect(this.logTable.getByText(model)).toBeVisible()

5. `e2e/pages/SettingsPage.ts` — class `SettingsPage`:
   - Locators: `exportButton` (getByRole('button', { name: /export/i })), `importButton` (getByRole('button', { name: /import/i })), `importFileInput` (locator('input[type="file"]'))
   - Methods: `async goto()` → page.goto('/settings'), `async exportConfig()` clicks export, waits for download event, returns download path, `async importConfig(filePath)` uses setInputFiles on importFileInput
  </action>
  <verify>
    <automated>npx tsx -e "import('./e2e/pages/StatusPage.ts'); import('./e2e/pages/ProviderForm.ts'); import('./e2e/pages/ModelMappingPage.ts'); import('./e2e/pages/RoutingLogPage.ts'); import('./e2e/pages/SettingsPage.ts'); console.log('All page objects import OK')"</automated>
  </verify>
  <done>
    - All 5 page object files exist in e2e/pages/
    - Each exports a class with constructor(page: Page)
    - Each uses getByRole/getByLabel selectors (no CSS selectors)
    - TypeScript compilation succeeds (no import errors)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create E2E test files covering all flows</name>
  <files>e2e/tests/01-happy-path.spec.ts, e2e/tests/02-provider-types.spec.ts, e2e/tests/03-edge-cases.spec.ts, e2e/tests/04-config-export.spec.ts</files>
  <read_first>
    - e2e/pages/StatusPage.ts (just created)
    - e2e/pages/ProviderForm.ts (just created)
    - e2e/pages/ModelMappingPage.ts (just created)
    - e2e/pages/RoutingLogPage.ts (just created)
    - e2e/pages/SettingsPage.ts (just created)
    - e2e/playwright.config.ts (test configuration)
    - packages/proxy/tests/routes/admin.test.ts (test structure pattern)
    - .planning/phases/06-testing-documentation/06-PATTERNS.md (E2E test patterns)
  </read_first>
  <action>
Create 4 E2E test files. Each test must use Page Object Models from e2e/pages/. Tag smoke tests with `@smoke`.

1. `e2e/tests/01-happy-path.spec.ts` — Full flow test (@smoke):
   - test.describe('Happy Path')
   - test('full flow: navigate → verify status page loads', async ({ page }):
     - Create StatusPagePage, call goto(), waitForStatusReady()
     - expect page URL to contain '/' or health card to be visible
   - test('add provider → verify appears in list', async ({ page }):
     - Create ProviderFormPage, fillProvider('test-provider', 'https://api.test.com/v1', 'test-key-12345', 'OpenRouter')
     - Call save()
     - Navigate back to status page, expect 'test-provider' text to be visible

2. `e2e/tests/02-provider-types.spec.ts` — Each provider type (@smoke):
   - test.describe('Provider Types')
   - Array of providers: OpenRouter (baseUrl: https://openrouter.ai/api/v1), OpenCode (baseUrl: https://api.opencode.ai/v1), Ollama (baseUrl: http://localhost:11434), Custom (baseUrl: https://api.custom-ai.com/v1)
   - For each provider: test('should configure {name} provider', async ({ page }):
     - Create ProviderFormPage, fillProvider with provider details
     - Call save()
     - expect page.getByText(provider.name).toBeVisible()

3. `e2e/tests/03-edge-cases.spec.ts` — Edge cases (per D-75):
   - test.describe('Edge Cases')
   - test('should handle provider unavailable', async ({ page }):
     - Configure provider with invalid URL (https://invalid-provider-12345.com/v1)
     - Click Test Connection
     - expect error toast or error message to be visible (getByText with 'error', 'failed', or 'unavailable')
   - test('should display rate limiting warning', async ({ page }):
     - Navigate to status page
     - Verify health card displays provider status (warning badge if rate limited from Phase 5)
   - test('should show retry log entry', async ({ page }):
     - Navigate to routing log page
     - Verify log table is visible and contains entries

4. `e2e/tests/04-config-export.spec.ts` — Export/import flow:
   - test.describe('Config Export/Import')
   - test('should export config as JSON', async ({ page }):
     - Create SettingsPage, call goto()
     - Click export button, wait for download
     - expect download path to be defined
   - test('should import config from JSON file', async ({ page }):
     - Create SettingsPage, call goto()
     - Use importFileInput.setInputFiles with a test JSON file
     - expect success toast or confirmation message to be visible
  </action>
  <verify>
    <automated>npx playwright test --list 2>&1 | grep -c "spec.ts" | grep -v '^0$'</automated>
  </verify>
  <done>
    - 4 test files exist in e2e/tests/
    - 01-happy-path.spec.ts contains at least 2 tests with @smoke tag
    - 02-provider-types.spec.ts contains tests for all 4 provider types (OpenRouter, OpenCode, Ollama, Custom)
    - 03-edge-cases.spec.ts contains tests for provider unavailable, rate limiting warning, retry log
    - 04-config-export.spec.ts contains tests for export and import config
    - All tests import from '@playwright/test' and use Page Object classes from '../pages/'
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test → Express proxy | Test fixtures use mock/test API keys only, never real keys |
| E2E test → filesystem | Test config isolated to /tmp/claude-proxy-e2e-test, cleaned up in teardown |
| E2E test → macOS Keychain | Test teardown must delete any test Keychain entries created during tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | E2E test fixtures | mitigate | Use only test keys like 'test-key-12345' in test code; grep for 'sk-' pattern in e2e/ to verify no real keys |
| T-06-02 | Information Disclosure | E2E test teardown | mitigate | globalTeardown must rm -rf TEST_CONFIG_DIR and delete test Keychain entries with service name 'claude-code-proxy' |
| T-06-03 | Information Disclosure | E2E screenshots/videos on failure | mitigate | screenshot: 'only-on-failure' and video: 'retain-on-failure' in playwright config — ensure CI artifacts are not publicly accessible |
</threat_model>

<verification>
- Run `npx playwright test --list` to verify all tests are discovered
- Run `npx playwright test --project=chromium --grep "@smoke"` for quick smoke test validation
- Verify no real API keys in e2e/ directory: `grep -r 'sk-[a-zA-Z0-9]' e2e/` returns 0 matches
- Verify test config isolation: `grep -c 'CONFIG_DIR' e2e/fixtures.ts` >= 1
</verification>

<success_criteria>
- Playwright E2E framework installed and configured with webServer for both Express proxy and Next.js
- 5 Page Object Models created for all UI pages (StatusPage, ProviderForm, ModelMappingPage, RoutingLogPage, SettingsPage)
- 4 E2E test files covering: happy path, all 4 provider types, edge cases (provider unavailable, rate limiting, retry), config export/import
- Tests use Page Object Models exclusively (no inline selectors)
- Test config state isolated to /tmp/claude-proxy-e2e-test with cleanup in teardown
- npm scripts added: test:e2e, test:e2e:smoke, test:e2e:ui
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-documentation/06-01-SUMMARY.md`
</output>

export default function RootLoading() {
  return (
    <div className="min-h-screen bg-canvas flex items-center justify-center" role="status" aria-live="polite">
      <p className="text-body text-muted">Loading dashboard…</p>
    </div>
  );
}
